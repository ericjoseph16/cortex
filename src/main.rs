use std::error::Error;
use clap::Parser;
use std::fs;
use std::path::{Path, PathBuf};
use std::ffi::OsStr;

// #[derive(Parser)]
// struct Cli {
//     pattern: String,
//     path: std::path::PathBuf,
// }

fn main() {
    
}